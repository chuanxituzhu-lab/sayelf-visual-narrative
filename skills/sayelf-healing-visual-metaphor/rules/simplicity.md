# Rule: Simplicity

Keep the minimum visible system:

```text
one character + one world + one metaphor + one relationship + one change
```

If an element does not support one of those objects or interactions, remove it. A quiet frame is not an incomplete frame; it is the evidence that the selected relationship is carrying the meaning.
